# Phoenix Warm-Up Engine — Python dependencies
# Install with: pip install -r requirements.txt

# UI framework (modern tkinter replacement)
customtkinter>=5.2.2

# Background job scheduling
# Pin to 3.x — APScheduler 4.0 completely restructured its module paths
# and breaks all imports (executors.pool, triggers.interval, etc.)
APScheduler>=3.10.4,<4.0.0

# IMAP client for inbox scanning
imapclient>=2.3.1,<4.0.0

# Human-like content generation
Faker>=24.0.0

# Environment variable loading
python-dotenv>=1.0.1

# Image handling (for logo display in UI)
Pillow>=10.2.0

# PyInstaller for creating standalone executables
# (only needed for building the distributable)
pyinstaller>=6.5.0
